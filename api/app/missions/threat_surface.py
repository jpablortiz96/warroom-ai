"""Threat Surface mission — security and compliance exposure analysis.

Track 3: scans for leaked credentials, CVEs in the stack, regulatory flags, dark web mentions.
Day 2: Wire up the 5-agent LangGraph graph with threat-surface-specific prompts.
"""


async def run_mission(target: str, context: str | None = None) -> dict:
    raise NotImplementedError("Day 2")
