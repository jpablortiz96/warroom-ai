"""Account Pulse mission — GTM intelligence on target accounts.

Track 1: monitors competitor moves, hiring signals, funding events, product launches.
Day 2: Wire up the 5-agent LangGraph graph with account-pulse-specific prompts.
"""


async def run_mission(target: str, context: str | None = None) -> dict:
    raise NotImplementedError("Day 2")
